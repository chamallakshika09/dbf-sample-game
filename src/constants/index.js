const UPDATE_STATE = 'update-state';
const BROADCAST_UPDATED_STATE = 'broadcast-updated-state';
const SEND_INITIAL_STATE = 'send-initial-state';

module.exports = { UPDATE_STATE, BROADCAST_UPDATED_STATE, SEND_INITIAL_STATE };
